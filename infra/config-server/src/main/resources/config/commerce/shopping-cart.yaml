spring:
  application:
    name: shopping-cart

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://smart-home-tech-db:5432/sht?currentSchema=cart
    username: sh
    password: sh

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 0

logging.level.ru.yandex.practicum: DEBUG
